# Handoff: Faz 1 kapandı (plan/set uçları + dean-pt skill + Samsung arşivi)

> 2026-09-22 09:42 · `dev` @ `9449fad` · çalışma ağacı temiz · önceki: `2026-09-21-2208-tek-uygulama-ia.md`

## Hedef

Spec `docs/superpowers/specs/2026-09-21-tek-uygulama-ia-design.md` §9 Faz 1: şema + uçlar
+ `dean-pt` skill. Dean "devam onaylısın" dedi, sıra Faz 2'ye geldi.

## Yapılan — kanıtlı

- **`db/008_plan.sql`** uygulandı (`applied 008_plan.sql`): `workout_plan` (weekday = JS getDay)
  ve `exercise_set`. Commit `4a5e155`.
- **Uçlar** (`apps/api/src/routes.ts`): `GET/PUT /api/workout-plan` (split deseni — gönderilmeyen
  alana dokunulmaz), `GET /api/exercise-sets?exercise_id=&limit=`, `POST /api/workouts` gövdesinde
  opsiyonel `sets` (seans + setler **tek transaction**, `pool.connect()` + begin/commit/rollback),
  `GET /api/workouts` yanıtında `sets` dizisi, `/api/export` iki yeni tabloyu kapsıyor.
- **Canlı doğrulama:** `PUT → GET` aynı veriyi döndü (`fit.evaitec.com`), geçersiz `day_type` 400,
  test satırı sonra silindi (`DELETE 1`). API konteyneri `docker compose up -d --build api` ile
  yeniden kuruldu — yeni uçlar canlıda.
- **`~/.claude/skills/dean-pt/SKILL.md`** yazıldı, `validate-skill.py` → PASS, 98 satır.
  `~/.ai/vg.env`'e `WELLNESS_API_BASE` + `WELLNESS_API_TOKEN` eklendi; reçete `GET /api/goals`
  ile doğrulandı (`{"protein_g":180,...}` döndü). Skill commit'i `~/.claude` reposunda `998abdb`.
- **`ops/import_samsung.mjs`** + `ops/import_samsung.test.mjs` (commit `9449fad`).
  Koştu: `659 gun 2024-09-05 -> 2026-09-21`, `200 gun dolduruldu, 459 gun zaten doluydu`.
  DB: adım 659 gün, kilo 55 gün, tansiyon 13 gün, `wearable_sync` 736 satır `samsung_csv`.
- **`PROGRAM-2026-09.md` §1** tansiyon cümlesi 18 ölçümlük gerçek banda çevrildi (`ac321f8`).
  **Doğrulanmadı:** bandı (128–147/84–96) önceki oturumun arşiv raporundan aldım, zip'i
  bu oturumda yeniden analiz etmedim. Ama içe aktarma sonrası DB'de 18–21 Eyl TA
  141/88 · 138/85 · 132/89 · 132/88 görünüyor — cümleyi destekliyor.
- Kanıt komutları: `npm run typecheck --workspaces` → EXIT=0 · `apps/api` `pass 61 fail 0` ·
  `node --test ops/*.test.mjs` → `pass 3 fail 0`.

## Yol boyunca çıkan kök neden

`POST /api/wearable` aynı istekte mükerrer `(date, source, metric)` gelince Postgres 21000
(`ON CONFLICT DO UPDATE command cannot affect row a second time`) ile **500** veriyordu.
Günde iki tartı ya da iki tansiyon ölçümü bunu tetikler — telefondan gelen batch'te de
patlardı. Uçta `Map` ile tekilleştirildi (son ölçüm kazanır), testi yazıldı.

## Kararlar

- **Egzersiz seansları Samsung'dan alınmadı.** `exercise_type` kodlarının (0, 1001, 14001…)
  bizim `resistance/cardio/walk` tiplerine eşlemesi doğrulanamadı; 584 satır `needs_review`
  ile "bu neydi?" yağmuru olurdu. Script başlığında yazılı. İstenirse ayrı adım.
- **`daily_log`'a yalnız boş alan yazılır.** Arşiv eski, canlı kayıt (Health Connect / elle
  giriş) daha güvenilir. 19 Eyl adımı 7261 kaldı, Samsung'un 7486'sı üzerine yazmadı.
- **Rate limit gevşetilmedi** (300/dk, `server.ts MAX_PER_WINDOW`). İçe aktarma script'i
  429'da 60 sn bekleyip yeniden dener — tek seferlik iş için yeterli.
- Skill program değerlerini **kopyalamaz**, `docs/PROGRAM-2026-09.md` yolundan okur.

## Tekrarlanmayacaklar

- **Testleri ekledikten sonra typecheck'i tekrar koş.** İlk commit'te "typecheck temiz"
  dedim; test eklendikten sonra `noUncheckedIndexedAccess` üç hata veriyordu (`sets[0]`).
  `npm test` yeşil olması typecheck'in yeşil olduğunu göstermiyor.
- Bash heredoc içinde `\n` kaçışı: python heredoc'tan JS'e string yazarken `'...\n'`
  gerçek satır sonuna dönüşüp sözdizimini kırdı. Çok satırlı dosya → Write tool.
- `package.json`'a satır eklerken önceki satıra virgül: `npm error EJSONPARSE` aldım.
- Samsung adımı üç kez yazılı; `source_type=-2` dışını alma (script'te zaten filtreli, testi var).

## Açık işler

1. **Faz 2** — 4 sekme + drawer + Plan sekmesi + Tabak kartı. `App.tsx` şu an 5 sekme
   (`today/chat/moves/week/settings`), `TABS` dizisi ve `SCALE` ritmi orada; Hareket ve Ayar
   drawer'a taşınacak, `Week.tsx` → Ölçüm olacak, yeni `Plan.tsx`/`Drawer.tsx`/`DocPage.tsx`/
   `DayStrip.tsx`/`SessionCard.tsx`/`PlateCard.tsx`. Dal: `feature/ia-4-sekme`.
2. Faz 3 — SetRow, geçen sefer, dinlenme sayacı (uçlar hazır, sadece UI).
3. Önceki devirden: 0.26.0 APK cihaz kanıtı, keystore kalıcı yere, çekim rehberi.
4. Artifact `3b8b7694` (seçici) ve `d94aafee` (IA taslağı) Dean tarafından açılmadı — doğrulanmadı.

## Sıradaki tek adım

Faz 2'ye `feature/ia-4-sekme` dalında başla: önce `App.tsx` TABS'ı dörde indir + `Drawer.tsx`
ekle (Hareket/Ayar drawer'dan erişilebilir olmadan sekme kaldırılırsa kör nokta kalır —
spec incelemesinde çıkan hata buydu).
