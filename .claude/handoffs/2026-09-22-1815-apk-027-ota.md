# Handoff: 0.27.0 APK yayında · Faz 2'ye yeni oturumda devam

> 2026-09-22 18:15 · `dev` @ `f685c5a` + sürüm commit'i · çalışma ağacı temiz
> önceki: `2026-09-22-1544-beslenme-kurallari.md`

## Hedef

Dean: "yeni oturumda başlayalım fazlara, son durumu APK at, güncelleyelim OTA ile, sonra handoff."
Üçü de yapıldı. Faz 2'nin kalanı yeni oturuma.

## Yapılan — kanıtlı

- **`feature/ia-4-sekme` → `dev` squash-merge** (`f685c5a`), dal silindi. Sürüm
  `variables.gradle` içinde `0.26.0 → 0.27.0`.
- **APK'lar derlendi:** `BUILD SUCCESSFUL in 1m 49s` · `wellness-0.27.0.apk` 24 149 812 byte ·
  `wellness-wear-0.27.0.apk` 13 054 444 byte.
- **GitHub release `v0.27.0`** (life-os-wellness) iki APK ile açıldı — `gh release view` doğruladı.
- **OTA kataloğu güncel:** `gh api .../apps.json` → `wellness-phone 0.27.0 / 2700` ve
  `wellness-wear 0.27.0 / 2700`. evaglass-releases'te `wellness-v0.27.0` release'i var.
  (`raw.githubusercontent.com` beş dakika bayat gösteriyor — katalogu **gh api** ile doğrula.)
- **İndirme çalışıyor:** `https://fit.evaitec.com/ota/wellness-0.27.0.apk` → HTTP 206, 1 MB parça indi.
- `git push origin dev v0.27.0` tamamlandı (exit 0); `git status -sb` ile dal durumu doğrulandı.

## 0.27.0'da ne var

Dört sekme (Bugün/Plan/Ölçüm/Koç) + drawer (Hareket kütüphanesi, Ayarlar) · Plan sekmesinden
gün tipi yazma (`PUT /api/workout-plan`) · sade görsel dil (aurora glow ve gradyan yazı yok) ·
Faz 1 uçları (`workout-plan`, `exercise-sets`, `workouts` sets desteği) · Samsung içe aktarımı ·
9 yeni ürün kataloğu · PROGRAM §4 beslenme kuralları.

## Telefonda bakılacak (cihaz kanıtı yok)

Uygulama "Güncelle" düğmesini gösteriyor mu, 0.27.0 kuruluyor mu, dört sekme ve drawer
telefonda nasıl duruyor. **Görsel yön hâlâ Dean'in onayında.**

## Tekrarlanmayacaklar

- OTA katalogunu `raw.githubusercontent.com` ile doğrulama — CDN bayat. `gh api repos/.../contents/apps.json`.
- `publish_ota.mjs` bu deponun `v<sürüm>` release'inden APK çeker: **önce release açılmalı**, sonra script.
- Docker dist hash'i yerelden farklı (içeriği grep'le doğrula) · `curl -d` Türkçe karakteri bozuyor ·
  heredoc'tan JS'e `\n` yazma.

## Açık işler

1. **Faz 2 kalanı:** `ui/DayStrip.tsx`, `ui/SessionCard.tsx`, `ui/PlateCard.tsx`, Plan'da zar
   (`lib/plan.ts`, seçiciden taşınacak) + kas haritası, drawer doküman sayfaları (`DocPage.tsx`).
   Spec: `docs/superpowers/specs/2026-09-21-tek-uygulama-ia-design.md` §3–§4.
2. **Faz 3:** SetRow, geçen sefer, dinlenme sayacı — uçlar hazır.
3. Dean'den bekleyen: görsel yön onayı · yeni tansiyon değerleri (kayıtlı son ölçüm 21 Eyl 132/88,
   7 gün ort 136/88) · 19 Eylül'de eksik dediği öğün (o günde 5 öğün / 145 g kayıtlı) ·
   Samsung'da karşılığı olmayan 11 kod kararı · saatte A/B/A′ rutinlerinin kurulması.

## Sıradaki tek adım

Yeni oturumda Faz 2: `feature/bugun-kartlari` dalında `ui/DayStrip.tsx` + `ui/SessionCard.tsx`
ile Bugün sekmesini kur (spec §3.1, §3.3).
