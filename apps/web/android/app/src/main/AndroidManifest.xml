<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <service
            android:name=".SleepService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Hareket gecisleri: Play Services olay geldiginde tetikler, surekli
             dinleyen servis yok. exported=false: yalniz sistem gonderir. -->
        <receiver
            android:name=".ActivityTransitionReceiver"
            android:exported="false" />

        <!-- Saatteki uygulama olcum yollar. DataClient kalici: telefon kapali/uzaktayken
             kayit saatte birikir, baglanti gelince bu servis uyanip kuyruga alir.
             Yol sozlesmesi: wear/src/main/java/com/evaitec/wellness/wear/WearMetrics.kt. -->
        <service
            android:name=".WearBridgeService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/wellness/metrics" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    
        <!-- Health Connect opens this when the user taps the privacy link (Android <= 13). -->
        <activity
            android:name="com.fit_up.health.capacitor.PermissionsRationaleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- Android 14+ uses an activity-alias for the same rationale screen. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity="com.fit_up.health.capacitor.PermissionsRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- OTA: telefon kendi guncellemesini indirip kurulum istemini acar. Kurulum yine
         sistem yukleyicisinin onayindan geciyor; sessiz kurulum yalniz device-owner'da
         mumkun. Dosyayi paylasan FileProvider asagida (${applicationId}.fileprovider),
         izin verdigi kok res/xml/file_paths.xml icindeki cache-path. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Night snore monitor: microphone in a foreground service, audio never stored. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Hareket tanima (ActivityPlugin): telefon yurume/kosu/bisiklet/aracta/hareketsiz
         gecislerini kendisi bilir. Android 10+ runtime izni, oncesinde gms izni. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />

    <!-- Health Connect: only what the watch actually gives us (see src/lib/health.ts). -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />

    <!-- capacitor-health bu ikisini okuyamiyor, kendi HealthExtra eklentimiz okuyor. -->
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />

    <!-- Kan oksijeni, HRV, uyku ve beslenme: capacitor-health'in izin listesinde yok,
         onayi HealthExtraPlugin kendisi istiyor. Health Connect'te stres kaydi yok;
         stresin en yakin olcusu HRV. Beslenmeden yalniz protein grami okunuyor
         (AGENTS.md: kalori/besin veritabani kapsam disi). -->
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_NUTRITION" />

    <!-- Kan basincini uretmiyoruz, mansonlu cihazdan/saatten geleni okuyoruz
         (docs/SENSORS-FEASIBILITY.md 2.1: mansonsuz KB dogrulanmis bir olcum degil). -->
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />

    <!-- The plugin library declares more health permissions than we read; drop them so the
         Health Connect consent screen only lists what src/lib/health.ts actually asks for. -->
    <uses-permission android:name="android.permission.health.READ_DISTANCE" tools:node="remove" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE_ROUTE" tools:node="remove" />
    <uses-permission android:name="android.permission.health.READ_HEIGHT" tools:node="remove" />
    <uses-permission android:name="android.permission.health.READ_BODY_FAT" tools:node="remove" />
    <uses-permission android:name="android.permission.health.READ_LEAN_BODY_MASS" tools:node="remove" />

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

</manifest>
